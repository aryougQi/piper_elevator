# ROS 2 DDS domain. Both computers must use the same value.
ROS_DOMAIN_ID=0

# Keep these empty when no proxy is needed.
# When using Clash Verge with host networking, 127.0.0.1 points to the host.
HTTP_PROXY=http://127.0.0.1:7897
HTTPS_PROXY=http://127.0.0.1:7897
NO_PROXY=localhost,127.0.0.1,::1

